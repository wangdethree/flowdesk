# 虚拟环境和缓存不应该进入镜像，镜像内会重新安装 requirements.txt。
.venv/
__pycache__/
*.py[cod]

# 本地数据库和本地环境变量只属于开发机，不打进镜像。
db.sqlite3
.env

# Git、IDE 和系统文件不影响容器运行，排除后镜像构建上下文更小。
.git/
.idea/
.DS_Store

# 文档不是运行服务必需文件，后续如果要在镜像里提供文档可以再放开。
docs/
